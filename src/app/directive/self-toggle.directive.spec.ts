import { SelfToggleDirective } from './self-toggle.directive';

describe('SelfToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SelfToggleDirective();
    expect(directive).toBeTruthy();
  });
});
