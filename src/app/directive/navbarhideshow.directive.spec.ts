import { NavbarhideshowDirective } from './navbarhideshow.directive';

describe('NavbarhideshowDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarhideshowDirective();
    expect(directive).toBeTruthy();
  });
});
